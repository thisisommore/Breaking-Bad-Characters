export default interface Chatactor {
  id: string
  name: string
  portrayed: string
  image_url: string
  full_name: string
  birth_date: string
  occupation: string[]
  episodes_count: string
  series: string
  appearances: string[]
}
